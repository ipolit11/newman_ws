#!/bin/bash

# This script starts a terminal and runs the command "roslaunch face_tracker_newman start_automatic_tracking.launch"

# Open a terminal
#gnome-termin#al
#terminator
# Navigate to the directory containing the launch file
#cd /home/robot/newman_ws/

# Run the launch file
roslaunch face_tracker_newman start_automatic_tracking.launch
